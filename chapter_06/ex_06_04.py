glossary = {
	'list': 'object that holds an ordered set of values',
	'dictionary': 'object that holds key value pairs'
                  ' in unspecified order',
    'float': 'object holding decimal number value',
    'int': 'object holding integer number value',
    'string': 'object holding a string of characters',
	}

for word, explanation in glossary.items():
    print(word + ": " + explanation)